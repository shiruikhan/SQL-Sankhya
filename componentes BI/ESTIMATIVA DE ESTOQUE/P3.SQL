SELECT IPA.IDIPROC,
	IPA.NROLOTE,
	IPA.QTDPRODUZIR-NVL((SELECT SUM(I.QTDNEG) FROM TGFCAB C INNER JOIN TGFITE I ON (C.NUNOTA=I.NUNOTA) WHERE C.TIPMOV='F' AND C.IDIPROC= PROC.IDIPROC AND I.ATUALESTOQUE=1 AND I.CODPROD = IPA.CODPRODPA AND C.CODTIPOPER <> 900),0) AS QTDPRODUZIR ,
	PROC.DTPREVENT,
	PROC.NUMPS
FROM TPRIPROC PROC 
	INNER JOIN TPRIPA IPA ON (PROC.IDIPROC = IPA.IDIPROC)
WHERE PROC.STATUSPROC IN ('A','P2')
	AND IPA.CODPRODPA = :A_CODPROD